name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - biopython >= 1.8
  - importlib-resources

    # Pip-only installs
  - pip:
    - logomaker
    - alfpy
    - torch
    - torchvision
    - fair-esm
    - transformers

