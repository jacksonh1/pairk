name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - biopython

    # Pip-only installs
  - pip:
    - logomaker
    - alfpy

